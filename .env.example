# AI Service Configuration (Optional)
# Ginko can use AI to extract insights from your sessions
# Set one of these to enable AI-powered insight extraction:
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
OPENAI_API_KEY=sk-your-openai-key-here
GROK_API_KEY=xai-your-grok-key-here
# XAI_API_KEY=xai-your-grok-key-here  # Alternative env var for Grok

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=contextmcp
DB_USER=postgres
DB_PASSWORD=password

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_test_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Price IDs (created during setup)
# Run: npx tsx scripts/stripe-setup-seats.ts to create products
STRIPE_PRO_MONTHLY_PRICE_ID=price_pro_monthly
STRIPE_PRO_YEARLY_PRICE_ID=price_pro_yearly
STRIPE_TEAM_MONTHLY_PRICE_ID=price_team_monthly
STRIPE_TEAM_YEARLY_PRICE_ID=price_team_yearly
STRIPE_ENTERPRISE_MONTHLY_PRICE_ID=price_enterprise_monthly
STRIPE_ENTERPRISE_YEARLY_PRICE_ID=price_enterprise_yearly

# Application Configuration
NODE_ENV=development
PORT=3031
FRONTEND_URL=http://localhost:3000

# Authentication Configuration
# Set to 'true' to require authentication in development
REQUIRE_AUTH=false

# MCP Client Configuration
# API key for authentication (optional in development)
WATCHHILL_API_KEY=
CONTEXTMCP_API_URL=http://localhost:3031
CONTEXTMCP_TEAM_ID=team-dev
CONTEXTMCP_PROJECT_ID=proj-contextmcp

# Redis Configuration (optional - for rate limiting)
REDIS_URL=redis://localhost:6379

# Neo4j Knowledge Graph Configuration (ADR-044)
# Connection to Neo4j AuraDB or self-hosted instance
# Format: bolt://<host>:<port> or neo4j+s://<host>:<port> for AuraDB
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=devpassword123

# For Neo4j AuraDB (production):
# NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=<auradb-generated-password>

# For self-hosted production:
# NEO4J_URI=bolt://<hetzner-ip>:7687
# NEO4J_PASSWORD=<generate-strong-password>

# Note: Vector index requires 1024 dimensions for voyage-3.5 embeddings

# Graph API Configuration (for WriteDispatcher dual-write testing)
# Enable graph writes via deployed API
GINKO_GRAPH_ENABLED=false
GINKO_GRAPH_API_URL=https://app.ginkoai.com
# GINKO_GRAPH_TOKEN - Leave unset to use gk_ API key from ~/.ginko/auth.json (recommended).
# Setting this overrides the auth file token and can cause 403 errors if stale.
# GINKO_GRAPH_TOKEN=
GINKO_GRAPH_ID=your_graph_id_here
# Enable dual-write mode (writes to both graph and local filesystem)
GINKO_DUAL_WRITE=false

# Vercel Deployment Protection Bypass (for automated testing)
# Get from: https://vercel.com/your-team/your-project/settings/deployment-protection
VERCEL_AUTOMATION_BYPASS_SECRET=

# Self-Hosted Embeddings Service Configuration
# Deployed on Hetzner server alongside Neo4j
EMBEDDINGS_API_URL=http://178.156.182.99:8080
EMBEDDINGS_MODEL=sentence-transformers/all-mpnet-base-v2
EMBEDDINGS_DIMENSIONS=768

# For production with authentication (see docs/EMBEDDINGS-SECURITY-BEST-PRACTICES.md):
# EMBEDDINGS_API_URL=https://embeddings.yourdomain.com
# EMBEDDINGS_API_TOKEN=your_bearer_token_here

# For local development (if running embeddings locally):
# EMBEDDINGS_API_URL=http://localhost:8080

# Vector Embeddings (Voyage AI - ADR-045)
# High-quality embeddings for semantic search and vector similarity
# Get your API key: https://dash.voyageai.com
# Free tier: 200M tokens (sufficient for most development needs)
# Model: voyage-3.5 (1024 dimensions, optimized for code/documentation)
# Pricing: https://docs.voyageai.com/pricing (after free tier exhausted)
VOYAGE_API_KEY=your_voyage_api_key_here

# Vector configuration
VOYAGE_MODEL=voyage-3.5
VOYAGE_DIMENSIONS=1024

# Alternative: Use self-hosted embeddings (legacy, lower quality)
# Set VOYAGE_API_KEY to empty to fall back to self-hosted embeddings
# VOYAGE_API_KEY=