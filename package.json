{
  "name": "ginko",
  "version": "2.4.5",
  "private": true,
  "workspaces": [
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build -w @ginkoai/cli",
    "test": "npm run test -w @ginkoai/cli",
    "clean": "rm -rf packages/cli/dist packages/cli/node_modules node_modules"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
